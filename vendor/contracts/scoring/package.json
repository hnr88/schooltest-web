{
  "name": "@schooltest/scoring-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Portable reading-scoring contracts (score-req/1, score-resp/1, stored Result, ResultView v2, diagnostic export v2). ONE source of truth imported by schooltest-api, schooltest-web, schooltest-app and every HTTP test so the shapes cannot drift.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" }
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "peerDependencies": { "zod": "^4.4.3" },
  "devDependencies": { "typescript": "^5.9.3", "vitest": "^4.1.10", "zod": "^4.4.3" }
}
