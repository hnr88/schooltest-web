---
id: 24
title: E2E — a11y sweep + responsive + Google-BLOCKED documentation + regression
layer: a11y
kind: verify
slice: axe on all new pages, mobile+desktop sweeps, full-suite regression
target: tests/e2e/a11y-auth.spec.ts, .qa/screenshots/
contract: all
status: DONE
depends_on: [19, 20, 21, 22, 23]
---
## Objective
- axe (existing @axe-core/playwright) on /sign-in, /sign-up, /dashboard (authed) →
  zero serious/critical; 375px + 1280px sweeps (no h-scroll, 44px targets, focus order);
  screenshots to .qa/screenshots/ (sign-in, sign-up, dashboard, dashboard-mobile).
- Full existing suite must stay green (19 landing/DS specs from M1 — they must not
  regress with the new pages).
- Google flow: document the BLOCKED e2e (D5) in the spec file as a skipped test with
  the exact reason + what ships instead (button + callback + provider config) —
  test.skip with a comment, NOT a fake pass.
## Done criteria
- axe zero; sweeps pass; full suite green; the skip note exists with the D5 reason.
## Evidence
DONE — independently verified 2026-07-18 by an independent explore verifier.

Personally ran `tests/e2e/a11y-auth.spec.ts` against the real :3100 web / :5500 api /
Postgres / Mailpit stack (not just the builder's self-report): 10/10 passed + 1 skipped;
`--repeat-each=3` → 30/30 passed + 3 skipped, zero flakes. Full suite
(`pnpm exec playwright test`) re-run twice: both runs 71/71 passed + 1 skipped (72
total), zero failures, including the pre-existing M1 landing/design-system specs
(git-diff confirmed completely untouched by this task).

axe (`@axe-core/playwright`) is genuinely zero serious/critical on /sign-in, /sign-up,
/auth/google/callback (error state), and every swept /dashboard state (welcome,
students table, search panel, add-student dialog open) at both 375px and 1280px,
modulo the ONE named, logged, non-blocking exemption (`scrollable-region-focusable` on
shadcn's vendored `table-container` div, `TABLE_SCROLL_EXEMPTION`) — observed firing in
my own run's console output, correctly not silently dropped (Law 11: never edit
`src/components/ui/*`). Zero horizontal scroll on any swept page/state at either
viewport. Forward-tab focus-order assertions passed for /sign-in, /sign-up, /dashboard.
Hard ≥43px regression assertions passed for every task-12/13 primary control (email/
password/username/confirm-password inputs, show-password/show-confirm-password
toggles, submit buttons, Google link) at both viewports; every other undersized
control (shadcn vendored defaults in StudentsSection/AddStudentDialog/DashboardSearch,
the ~82×26 logo link) is advisory-logged only — matching `a11y-responsive.spec.ts`'s
pre-existing "(see D22)" comment at line 117, which genuinely predates this task
(confirmed via `git show HEAD:tests/e2e/a11y-responsive.spec.ts` and `git diff --stat`
showing zero changes to that file) and had no matching DECISIONS.md entry until this
task's D22 append — retroactively resolved, not fabricated after the fact.

Google OAuth BLOCKED reasoning (D5) re-verified independently: `GET
/api/connect/google` → real `400 {error:{status:400,message:"This provider is
disabled"}}` (curl-verified, matches CONTRACTS.md C-AUTH-GOOGLE); grepped
`schooltest-api/.env*` and the live api process's environment for
`GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` — none exist anywhere (only commented-out
placeholders in `.env.example`), confirming the `test.skip` reason is real, not
assumed; it names the exact reason and what already ships instead, never a fake pass.

Contract conformance re-curled live: `POST /api/auth/local` (seeded parent) → 200 jwt;
`GET /api/my/students` → 200 `{data:[Jonas(10),Mia(8)],meta.pagination.total:2}`,
checked both before and after the full suite ran — zero pollution from the
add-student-dialog tests' fresh throwaway parent registrations (D20's serial-mode
workaround holds).

`pnpm tsc --noEmit`: 0 errors. `pnpm lint`: 0 errors, 1 pre-existing unrelated warning
in untouched `CreateArticleForm.tsx` (same one every prior task logs). `prettier
--check` on the actual touched code file (`tests/e2e/a11y-auth.spec.ts`): clean; the
`DECISIONS.md` formatting warning is pre-existing markdown drift, confirmed via `git
stash`/`pop` to fire identically on the pre-D22 version too (not introduced by this
task, and it is a builder-log file, not code). `grep -niE
mock/fake/stub/dummy/placeholder/hardcod` across the new spec file: zero fixture hits
(only a comment referencing the pre-existing, already-established `stub-jwt`
literal-string pattern shared identically with sign-in/sign-up/google-callback/
dashboard specs — not a new fake).

No new write/persistence path in this task (pure e2e/a11y verification of already-DONE,
already-persisted flows), so no new DB-persistence-across-reload check applies. All 6
named screenshots (`a11y-sign-in-en.png`, `a11y-sign-in-mobile-en.png`,
`a11y-sign-up-en.png`, `a11y-sign-up-mobile-en.png`, `a11y-dashboard-en.png`,
`dashboard-mobile-en.png`) freshly regenerated by my own run in `.qa/screenshots/`.

Full evidence recorded in `.qa/STATE.json` task 24's `verify` object.
