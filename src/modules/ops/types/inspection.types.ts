/**
 * Ledger row 11 / D-007 web types.
 *
 * ops/43 (R-22): the form-inspection, view-as-teacher and responses-export
 * drill-ins this file's types served are retired — none is drawn anywhere in
 * `Ops Portal.dc.html:23-883`, and their components, queries and i18n keys
 * are retired alongside them. This file is kept empty rather than deleted
 * (it stays a valid extension point for a future browser-only ops type),
 * per the retirement register's file-survival rule.
 */
export {};
