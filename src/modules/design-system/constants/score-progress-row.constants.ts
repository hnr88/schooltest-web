import type { ScoreProgressTone } from '@/modules/design-system/types/record.types';

export const FILL_TONES: Record<ScoreProgressTone, string> = {
  primary: 'bg-primary',
  accent: 'bg-accent',
  success: 'bg-success',
  warning: 'bg-warning',
  danger: 'bg-destructive',
};
