/**
 * The `/dashboard/test-sessions` segment (.qa/DECISIONS.md A4 — ONE shell, the
 * teacher surface lives under `/dashboard`). The live monitoring view for a
 * single sitting hangs off it, so "Go live" and the C-TS-3 grid derive their
 * path from the same constant instead of each spelling one out.
 */
export const TEST_SESSIONS_PATH = '/dashboard/test-sessions';
