MEASURED, not inferred. Playwright CLI, chromium, workers=1, 2026-09-11.

--- 026-dashboard-onboarding-guard BEFORE the gate ---
Error: page.waitForURL: Test timeout of 30000ms exceeded.
waiting for navigation to "**/onboarding" until "load"
  navigated to "http://localhost:3002/dashboard"
  1 failed, 2 did not run

--- 026 AFTER the gate (skipWhenParentPortalMasked) ---
  3 skipped
  shared parent onboarding row still reads: skipped   <- beforeAll did NOT fire
  with NEXT_PUBLIC_PARENT_VIEWS_ENABLED=true: 3 tests collected  <- coverage gated, not deleted

--- unified-search (parent -> /dashboard/search, PREDICTED masked) ---
  5 failed   (every test in the file)

--- model check ---
The gate tests TEACHER_REPORTS_PATH before the parent branch, so a parent at
/dashboard/reports PASSES. teacher-report.spec.ts is therefore predicted to pass
and to need NO change — the negative control on the model.

--- teacher-report.spec.ts (the NEGATIVE CONTROL) ---
  4 passed, 2 failed
  The PARENT test ("a parent account gets no rail entry and is redirected off the
  report route") is among the PASSES — confirming the model: the gate tests
  TEACHER_REPORTS_PATH before the parent branch, so a parent at /dashboard/reports
  passes and that spec needs NO parent-gating.
  Its 2 failures are a DIFFERENT stale-decision case, not this one:
    :85  asserts the teacher "Reports" RAIL ENTRY — retired by R-11 (teacher-rail.ts
         records "with `reports` retired (R-11) a teacher has zero primary items")
    :132 a productive-skill (writing) result — a data-shape gap, not the gate
  Same family as Task 46 (spec outlived a recorded decision), different decision.

=== HANDOFF — the sweep was STOPPED here (orchestrator, 2026-09-11 03:27Z: teacher portal is now the priority) ===
BEFORE run 03:24:56–03:31:41Z. Playwright CLI, chromium, workers=1, retries=0, flag unset (masked).
Nothing was gated or edited in this batch. Candidate set: `git grep` at HEAD over tests/e2e for a parent
identity = 48 files; minus the 23 already calling skipWhenParentPortalMasked = 26 ungated, non-helper files.

FAILED under the mask — each waits for portal content that never renders:
  025-onboarding-route-verify:16             parent at /en/onboarding                  -> per-test gate (:36 passes)
  a11y-auth:562, :589, :625                  parent /dashboard, /dashboard/children    -> per-test gate
  agent-search-polish:34, :87                parent /dashboard/search?mode=agents      -> whole-file gate
  change-password:103                        /dashboard/settings                       -> whole-file gate (:47 not run: mutates a password)
  ops36-portal-lists-kit:96                  parent /dashboard/notifications           -> per-test gate (:64, :141 unaffected)
  parent-auth:34                             waits for the "Welcome back, parent" h1   -> per-test gate  PREDICTION MISS
  sign-in:136                                waits for the "Welcome back, parent" h1   -> per-test gate  PREDICTION MISS
  school-search-presentation:39, :118        parent /dashboard/search                  -> whole-file gate
  unified-search-states:46, :80, :152, :180  parent /dashboard/search                  -> whole-file gate (:152's 308 lands, then waits for cards)
  unified-search (measured earlier, 5/5)     parent /dashboard/search                  -> whole-file gate
  The two misses were predicted to survive on their URL check; the URL does survive — the heading does not.

PASSED — negative controls, as predicted:
  auth-roles-smoke x8, incl. role: parent    a URL-only landing check survives the mask
  025-onboarding-route-verify:36             anonymous -> /sign-in
  shell-a11y:75, :85                         the shell (dashboard/layout.tsx) renders around the masked screen

PASSED BUT SUSPECT — report, do not fix:
  push-subscription-security:93              predicted masked, yet passed. Its assertion is an ABSENCE ("no enabled
                                             no-op control") on /dashboard/settings, which the mask never renders:
                                             probably a vacuous pass.

NOT RUN (mutating or a 175s hook) — masked by the same truth table, judge per test:
  027:287, :301 (onboarding, inside a serial registration chain), 027:379 (/dashboard/children),
  verify-tasks-013-017-toast:143 (/dashboard/settings), zz-task117:342 (/dashboard/notifications).

LEAVE — asserts a URL, an API, or the masked state itself:
  forgot-reset, sign-up, parent-auth-errors, notification-api-security, teacher-report (its :85 is R-11, not the
  mask), zz-task27:97 (asserts the masked state), zz-task75/76/77/78 (parent used only in API role matrices).

Per-test precedent: an in-body conditional skip is already idiomatic here (zz-task117:257, :314); a file gate
is right only where EVERY test is masked. Place it before any beforeAll that writes shared state.
